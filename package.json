{
  "name": "peculiar-certificates-viewer-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "keywords": [
    "certificates-viewer",
    "certificate-viewer",
    "certificate",
    "x509",
    "decoding",
    "web-component",
    "webpki",
    "pki",
    "asn1"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "clean": "lerna run --parallel clean",
    "lint": "eslint",
    "dev": "npm run --workspace @peculiar/certificates-viewer dev",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "@peculiar/eslint-config-base": "^0.2.9",
    "@peculiar/eslint-config-react": "^0.2.9",
    "lerna": "^9.0.7"
  },
  "license": "MIT",
  "author": "PeculiarVentures Team",
  "homepage": "https://x509.io/",
  "devEngines": {
    "runtime": {
      "name": "node",
      "version": ">=24",
      "onFail": "warn"
    },
    "packageManager": {
      "name": "npm",
      "version": "11.7.0",
      "onFail": "warn"
    }
  },
  "dependencies": {
    "ci": "^2.3.0"
  }
}
